import { LazyLoadAnimationDirective } from './lazy-load-animation.directive';

describe('LazyLoadAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
